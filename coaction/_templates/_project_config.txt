num_parallel_experiments = None  # 'None' means use all available cores. NOTE: consider available memory
num_parallel_episodes = None  # 'None' means use all available cores. This restricts the total number of parallel episodes in all experiments combined.
order = None  # list of experiment names that defines the run order of the experiments. Set to 'None' for lexicographic order
